package ObserverPattern;

import java.util.Observable;
import java.util.Observer;

/**
 * Die Klasse dient als Beispiel für einen Observer, der informiert wird, wenn Aenderungen im SubscriptionStateSubject auftreten.
 * @author Sandra Gabriela Hofmarcher
 *
 */

public class SubjectStateObserver implements Observer {

	
	@Override
	public void update(Observable o, Object arg) {	
	}

}
